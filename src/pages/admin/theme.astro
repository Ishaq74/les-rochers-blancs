---
import AdminLayout from '@/layouts/AdminLayout.astro';
import { t } from '@/lib/i18n';
export const prerender = false;
const lang = 'fr';
const i18n = {
  noSettings: t('settings.noSettings', lang),
};
---

<AdminLayout title={t('theme.title', lang)} activeNav="theme" lang={lang}>
  <div class="space-y-6">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
      <h1 class="text-2xl font-bold tracking-tight">{t('theme.title', lang)}</h1>
      <p class="text-muted-foreground">{t('theme.subtitle', lang)}</p>
      <div class="flex items-center gap-2 sm:ml-auto">
        <span id="pending-badge" class="hidden rounded-full border border-amber-300 bg-amber-50 px-2.5 py-1 text-xs font-medium text-amber-800"></span>
        <button id="btn-reset" type="button" class="inline-flex items-center rounded-md border border-input px-3 py-2 text-sm font-medium hover:bg-muted disabled:opacity-50" disabled>
          Annuler
        </button>
        <button id="btn-save-theme" type="button" class="inline-flex items-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90 disabled:opacity-60" disabled>
          Sauvegarder le theme
        </button>
      </div>
    </div>

    <div id="loading" class="p-8 text-center text-muted-foreground">{t('common.loading', lang)}</div>

    <div id="theme-form" class="hidden space-y-6">
      <div class="rounded-xl border border-border bg-card p-4 shadow-sm">
        <h2 class="text-sm font-semibold uppercase tracking-wider text-muted-foreground">Variables essentielles</h2>
        <p class="mt-1 text-sm text-muted-foreground">On privilegie ici les variables utiles et stables pour un theming coherent globalement.</p>
      </div>

      <div id="categories-container" class="space-y-8"></div>

      <div class="rounded-xl border border-border bg-card p-5 shadow-sm">
        <h3 class="text-sm font-semibold uppercase tracking-wider text-muted-foreground">Apercu rapide</h3>
        <div id="theme-preview" class="mt-3 rounded-xl border p-5" style="background: var(--background-color, #ffffff); color: var(--text-color, #111827); border-color: var(--border-color, #e5e7eb);">
          <p class="text-xs uppercase tracking-wider opacity-70">Brand UI Preview</p>
          <h4 class="mt-2 text-xl font-semibold">Les Rochers Blancs</h4>
          <p class="mt-1 text-sm opacity-80">Apercu direct des couleurs principales et du contraste.</p>
          <div class="mt-4 flex gap-2">
            <button type="button" class="rounded-md px-3 py-2 text-sm font-medium" style="background: var(--primary-color, #1a1a2e); color: var(--primary-foreground-color, #ffffff);">Bouton principal</button>
            <button type="button" class="rounded-md border px-3 py-2 text-sm font-medium" style="border-color: var(--border-color, #e5e7eb); color: var(--text-color, #111827); background: transparent;">Secondaire</button>
          </div>
        </div>
      </div>

      <div class="border-t border-border pt-6 space-y-4">
        <h3 class="text-sm font-semibold text-muted-foreground uppercase tracking-wider">{t('settings.addSetting', lang)}</h3>
        <div class="grid grid-cols-4 gap-4">
          <input id="new-key" placeholder={t('settings.form.keyPlaceholder', lang)} class="flex h-9 rounded-md border border-input bg-background px-3 text-sm" />
          <input id="new-value" placeholder={t('settings.form.value', lang)} class="flex h-9 rounded-md border border-input bg-background px-3 text-sm" />
          <select id="new-type" class="flex h-9 rounded-md border border-input bg-background px-3 text-sm">
            <option value="color">Couleur</option><option value="text">Texte</option><option value="number">Nombre</option><option value="font">Police</option>
          </select>
          <input id="new-category" placeholder={t('settings.form.category', lang)} value="general" class="flex h-9 rounded-md border border-input bg-background px-3 text-sm" />
        </div>
        <button id="btn-add" type="button" class="inline-flex items-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90">+ {t('common.add', lang)}</button>
      </div>
    </div>
  </div>

  <script is:inline define:vars={{ i18n }}>
    const API=(a,b)=>fetch(`/_actions/${a}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}).then(r=>r.json());

    const CORE_THEME_FIELDS=[
      { key:'primary-color', type:'color', category:'colors', label:'Couleur primaire' },
      { key:'primary-foreground-color', type:'color', category:'colors', label:'Texte sur primaire' },
      { key:'background-color', type:'color', category:'colors', label:'Fond global' },
      { key:'text-color', type:'color', category:'colors', label:'Texte principal' },
      { key:'border-color', type:'color', category:'colors', label:'Couleur des bordures' },
      { key:'radius-md', type:'text', category:'shape', label:'Rayon standard (ex: 0.75rem)' },
      { key:'shadow-soft', type:'text', category:'effects', label:'Ombre douce (CSS box-shadow)' },
      { key:'font-heading', type:'font', category:'typography', label:'Police titres' },
      { key:'font-body', type:'font', category:'typography', label:'Police texte' },
    ];

    let settings=[];
    const pendingValues=new Map();

    function byKey(key){ return settings.find(s=>s.settingKey===key); }

    function updateToolbar(){
      const hasChanges=pendingValues.size>0;
      const saveBtn=document.getElementById('btn-save-theme');
      const resetBtn=document.getElementById('btn-reset');
      const badge=document.getElementById('pending-badge');
      if(saveBtn)saveBtn.disabled=!hasChanges;
      if(resetBtn)resetBtn.disabled=!hasChanges;
      if(badge){
        badge.classList.toggle('hidden',!hasChanges);
        badge.textContent=hasChanges?`${pendingValues.size} changement(s) en attente`:'';
      }
    }

    async function load(){
      const r=await API('listThemeSettings',{});
      settings=r.data??r??[];
      pendingValues.clear();
      ensureCoreFields();
      render();
      updateToolbar();
    }

    function ensureCoreFields(){
      const existing=new Set(settings.map(s=>s.settingKey));
      CORE_THEME_FIELDS.forEach(f=>{
        if(existing.has(f.key)) return;
        settings.push({
          settingKey:f.key,
          settingValue:f.type==='color'?'#000000':'',
          settingType:f.type,
          category:f.category,
        });
      });
    }

    function previewValue(key){
      if(pendingValues.has(key)) return pendingValues.get(key);
      return byKey(key)?.settingValue || '';
    }

    function applyPreview(){
      const preview=document.getElementById('theme-preview');
      if(!preview) return;
      const vars={
        '--primary-color': previewValue('primary-color') || '#1a1a2e',
        '--primary-foreground-color': previewValue('primary-foreground-color') || '#ffffff',
        '--background-color': previewValue('background-color') || '#ffffff',
        '--text-color': previewValue('text-color') || '#111827',
        '--border-color': previewValue('border-color') || '#e5e7eb',
      };
      Object.entries(vars).forEach(([k,v])=>preview.style.setProperty(k,v));
      const radius=previewValue('radius-md');
      if(radius) preview.style.borderRadius=radius;
      const shadow=previewValue('shadow-soft');
      preview.style.boxShadow=shadow || 'none';
    }

    function render(){
      document.getElementById('loading')?.classList.add('hidden');
      document.getElementById('theme-form')?.classList.remove('hidden');

      const categories={};
      (settings||[]).forEach(s=>{
        const cat=s.category||'general';
        if(!categories[cat])categories[cat]=[];
        categories[cat].push(s);
      });

      const container=document.getElementById('categories-container');
      const cats=Object.keys(categories).sort();
      if(!cats.length){container.innerHTML=`<p class="text-muted-foreground">${escapeHtml(i18n.noSettings)}</p>`;return;}

      container.innerHTML=cats.map(cat=>`
        <div class="rounded-xl border border-border bg-card shadow-sm overflow-hidden">
          <div class="border-b border-border bg-muted/50 px-4 py-3"><h3 class="text-sm font-semibold uppercase tracking-wider">${escapeHtml(cat)}</h3></div>
          <div class="p-4 space-y-3">
            ${categories[cat].map(s=>`
              <div class="flex items-center gap-4">
                <label class="text-sm font-medium w-64 shrink-0">${escapeHtml(s.settingKey)}</label>
                ${s.settingType==='color'
                  ? `<input type="color" value="${escapeHtml(s.settingValue||'#000000')}" oninput="setThemeValue('${escapeHtml(s.settingKey)}',this.value)" class="h-9 w-12 rounded border border-input cursor-pointer" /><span class="text-xs text-muted-foreground">${escapeHtml(s.settingValue||'')}</span>`
                  : `<input type="text" value="${escapeHtml(s.settingValue||'')}" oninput="setThemeValue('${escapeHtml(s.settingKey)}',this.value)" class="flex h-9 w-full max-w-xl rounded-md border border-input bg-background px-3 text-sm" />`
                }
              </div>`).join('')}
          </div>
        </div>`).join('');
      applyPreview();
    }

    window.setThemeValue=function(key,value){
      const setting=byKey(key);
      if(!setting) return;
      if((setting.settingValue||'')===value) pendingValues.delete(key);
      else pendingValues.set(key,value);
      updateToolbar();
      applyPreview();
    };

    document.getElementById('btn-reset')?.addEventListener('click',()=>{
      pendingValues.clear();
      render();
      updateToolbar();
    });

    document.getElementById('btn-save-theme')?.addEventListener('click',async()=>{
      if(!pendingValues.size) return;
      const btn=document.getElementById('btn-save-theme');
      btn.disabled=true;
      btn.textContent='Sauvegarde...';
      try{
        const updates=settings
          .filter(s=>pendingValues.has(s.settingKey))
          .map(s=>API('saveThemeSetting',{
            settingKey:s.settingKey,
            settingValue:pendingValues.get(s.settingKey),
            settingType:s.settingType||'text',
            category:s.category||'general',
          }));
        await Promise.all(updates);
        showToast('Theme sauvegarde', 'success');
        await load();
      }catch(_e){
        showToast('Erreur de sauvegarde theme', 'destructive');
      }finally{
        btn.textContent='Sauvegarder le theme';
        updateToolbar();
      }
    });

    document.getElementById('btn-add')?.addEventListener('click',async()=>{
      const key=document.getElementById('new-key')?.value?.trim();
      const value=document.getElementById('new-value')?.value?.trim();
      const type=document.getElementById('new-type')?.value||'color';
      const category=document.getElementById('new-category')?.value?.trim()||'general';
      if(!key){
        showToast('Cle requise', 'destructive');
        return;
      }
      await API('saveThemeSetting',{settingKey:key,settingValue:value||'',settingType:type,category});
      document.getElementById('new-key').value='';document.getElementById('new-value').value='';
      showToast('Variable ajoutee', 'success');
      await load();
    });

    load();
  </script>
</AdminLayout>
